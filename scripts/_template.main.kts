#!/usr/bin/env kotlin

// This is a template file from scripts. Saves you the trouble of having to chmod everytime,
// by simply copy-pasting and renaming this file.

// I should probably add some more instructions here, when I figure out more

println("Hello World")

// We can also access arguments
// call the script with
println(args.toList())